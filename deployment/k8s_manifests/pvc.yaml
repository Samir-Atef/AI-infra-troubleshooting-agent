apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: infra-agent-chroma-pvc
  namespace: infra-agent
  labels:
    app.kubernetes.io/name: infra-troubleshooting-agent
    app.kubernetes.io/component: vector-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard
