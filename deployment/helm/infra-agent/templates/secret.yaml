apiVersion: v1
kind: Secret
metadata:
  name: {{ include "infra-agent.fullname" . }}-secrets
  labels:
    {{- include "infra-agent.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
type: Opaque
stringData:
  AGENT_LLM_API_KEY: {{ .Values.secrets.llmApiKey | quote }}
